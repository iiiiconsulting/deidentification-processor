# Contracts schema
# Source columns: name, customerName, interval, every, on, plan, amount, status, lastInvoiceDate, nextBillDate

identity:
  source: customerName

columns:
  customerName: hash
  name: pass
  interval: pass
  every: pass
  "on": pass
  plan: pass
  amount: pass
  status: pass
  lastInvoiceDate: pass
  nextBillDate: pass
